# 🔴 紧急 Bug 通知 - qclaw-tester

**时间:** 2026-03-20 00:03  
**来源:** qclaw-tester  
**优先级:** P0 - 阻塞测试执行

---

## 📊 当前测试状态

| 测试套件 | 通过 | 失败 | 跳过 | 通过率 | 状态 |
|---------|------|------|------|--------|------|
| Functional | 5 | 0 | 0 | 100% | ✅ 完美 |
| Integration | 64 | 6 | 0 | 91.4% | ⚠️ 良好 |
| Unit | 0 | 0 | 0 | - | ❌ 收集错误 |
| System | 22 | 7 | 10 | 75.9% | ⚠️ 部分通过 |
| Performance | 17 | 3 | 0 | 85% | ⚠️ 良好 |
| E2E | - | - | - | - | ❌ 阻塞 |

**服务状态:** API ✅ (8000, 3h) | Frontend ✅ (80, 3h) | Redis ✅ (6379, 7h)

---

## 🔴 P0 阻塞问题 (立即修复)

### 1. Unit 测试导入路径错误

**错误:** `ModuleNotFoundError: No module named 'api'`  
**位置:** `server/main.py:15`  
**代码:** `from api import market_router, health_router, advice_router, dl_models_router, dl_predict_router`  
**影响:** 4 个 API 单元测试无法执行 (test_advice_api.py, test_dl_api.py, test_health_api.py, test_market_api.py)  

**修复方案 A (推荐):** 更新 `pytest.ini`:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
env = PYTHONPATH=server
```

**修复方案 B:** 修改导入路径:
```python
# server/main.py:15
from server.api import market_router, health_router, advice_router, dl_models_router, dl_predict_router
```

---

### 2. Integration 测试缺少模块

**错误:** `ModuleNotFoundError: No module named 'src.prediction.data.tft_adapter'`  
**位置:** `tests/integration/test_model_integration.py`  
**影响:** test_data_pipeline_integration 失败  

**修复:**
- 方案 A: 创建缺失模块 `src/prediction/data/tft_adapter.py`
- 方案 B: 更新导入路径或使用 mock

---

### 3. E2E 测试端口配置错误

**文件:** 
- `tests/e2e/test_user_flows_updated.py` (13 处)
- `tests/e2e/test_edge_cases.py` (1 处 BASE_URL)
- `tests/e2e/test_error_handling.py` (1 处 BASE_URL)

**问题:** 测试使用 `localhost:3000`，实际前端运行在 `localhost:80`  
**影响:** 约 46 个 E2E 测试失败  

**修复:**
```bash
cd ~/qclaw/tests/e2e
sed -i 's/localhost:3000/localhost:80/g' test_user_flows_updated.py
sed -i 's/localhost:3000/localhost:80/g' test_edge_cases.py
sed -i 's/localhost:3000/localhost:80/g' test_error_handling.py
```

---

## 🟠 P1 问题 (尽快修复)

### 4. System 测试缺少数据文件

**缺少:**
- `data/real/600519_贵州茅台.csv`
- `checkpoints/lstm_real_600519.pth`

**影响:** test_full_pipeline.py 5 个测试失败  
**修复:** 准备测试数据文件或添加数据下载脚本

---

### 5. System 测试健康检查端点不匹配

**测试期望:** `/health` 端点返回 200  
**实际:** 健康检查在 `/` 端点，`/health` 返回 404  
**影响:** test_api_health_endpoint 失败  

**修复方案 A:** 添加 `/health` 端点:
```python
# server/api/health_router.py
@router.get("/health")
async def health_check():
    return {"name": "QCLaw 量化交易平台", "version": "1.0.0", "status": "running"}
```

**修复方案 B:** 更新测试使用 `/` 端点

---

### 6. TFT 性能测试代码错误

**错误:** `TypeError: tuple indices must be integers or slices, not tuple`  
**位置:** `tests/performance/test_tft_performance.py:36`  
**代码:** `errors = target - y_pred[..., i]`  
**影响:** 2 个 TFT 测试失败 (test_training_speed, test_prediction_accuracy)  

**修复:** 检查 TFT 模型输出格式，正确解包 output tuple

---

### 7. Integration 测试缓存实现问题

**问题:**
- LRU 缓存驱逐逻辑错误 (test_cache_lru_eviction)
- 缓存返回对象比较问题 (test_pipeline_cache_hit)  
- 缓存加速比未达预期 1.56x < 5x (test_cache_performance)
- 并行性能退化 0.02x (test_parallel_performance)

**位置:** `src/data/pipeline_optimized.py`  
**影响:** 4 个 Integration 测试失败  
**修复:** 审查缓存实现逻辑

---

## 🟡 P2 问题 (有空修复)

### 8. System 测试逻辑问题

**文件:** `tests/system/test_load_stress.py:158`  
**问题:** assert 语句在 `pass` 后 (不可达代码)  
**影响:** test_error_recovery 失败  

**修复:** 移除 `pass` 或重构测试逻辑

---

### 9. Performance 测试 API 并发失败

**问题:** API 并发成功率 0% < 80% 预期  
**位置:** `tests/performance/test_perf_001.py`  
**影响:** test_api_concurrent_requests 失败  
**修复:** 检查 API 并发处理能力或调整测试配置

---

## 📋 修复后验证步骤

```bash
# 1. 验证 Unit 测试
cd ~/qclaw
python3 -m pytest tests/unit/api/ -v

# 2. 验证 Integration 测试
python3 -m pytest tests/integration/ -v

# 3. 验证 E2E 测试
cd ~/qclaw/tests/e2e
npx playwright test --reporter=list

# 4. 验证 System 测试
cd ~/qclaw
python3 -m pytest tests/system/ -v

# 5. 验证 Performance 测试
python3 -m pytest tests/performance/ -v
```

---

## 📝 备注

- Functional 测试保持 100% 通过 ✅
- Docker 容器全部正常运行 (已运行 3 小时)
- 浏览器工具不可用，CODE-009 UI 验证暂时跳过
- 详细报告：`docs/reports/tester-status-2026-03-20-0003.md`

**状态:** 🔴 阻塞中 - 等待 Coder 修复 P0 问题  
**下次检查:** 2026-03-20 01:00 (Cron 自动)
