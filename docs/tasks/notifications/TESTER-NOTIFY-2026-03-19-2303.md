# 🔴 紧急 Bug 通知 - qclaw-tester

**时间:** 2026-03-19 23:03  
**来源:** qclaw-tester  
**优先级:** P0 - 阻塞测试执行

---

## 📊 当前测试状态

| 测试套件 | 通过 | 失败 | 通过率 | 状态 |
|---------|------|------|--------|------|
| Functional | 5 | 0 | 100% | ✅ 完美 |
| Integration | - | - | - | ❌ 收集错误 |
| Unit | 0 | 0 | - | ❌ 导入错误 |
| System | - | - | - | ❌ 语法错误 |
| E2E | - | - | - | ❌ 端口配置 |
| Performance | 10 | 10 | 50% | ⚠️ 部分通过 |

**服务状态:** API ✅ (8000) | Frontend ✅ (80) | Redis ✅ (6379)

---

## 🔴 P0 阻塞问题 (立即修复)

### 1. E2E 测试端口配置错误

**文件:** `tests/e2e/test_user_flows.py`  
**问题:** 18 处使用 `localhost:3000`，实际前端运行在 `localhost:80`  
**影响:** 46 个 E2E 测试全部失败  
**修复:**
```bash
cd ~/qclaw/tests/e2e
sed -i 's/localhost:3000/localhost:80/g' test_user_flows.py
```

### 2. Unit 测试导入路径错误

**错误:** `ModuleNotFoundError: No module named 'api'`  
**位置:** `server/main.py` 第 15 行 `from api import ...`  
**影响:** 4 个 API 单元测试无法执行  
**修复方案 A (推荐):** 更新 `pytest.ini`:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
env = PYTHONPATH=server
```

**修复方案 B:** 修改导入路径为 `from server.api import ...`

### 3. Integration 测试缺少文件

**缺少:** `src/data/pipeline_optimized.py`  
**影响:** `test_pipeline_optimized.py` 无法收集  
**修复:** 创建文件或移除相关测试

---

## 🟠 P1 问题 (尽快修复)

### 4. System 测试语法错误

**文件:** `tests/system/test_load_stress.py:161`  
**问题:** `raise` 语句缩进错误  
**当前代码:**
```python
        # 重试逻辑已简化，跳过此测试
        # 重试逻辑已简化，跳过此测试
        # 重试逻辑已简化，跳过此测试
                    raise
```
**修复:** 删除多余缩进或注释掉该行

### 5. System 测试缺少数据文件

**缺少:**
- `data/real/600519_贵州茅台.csv`
- `checkpoints/lstm_real_600519.pth`

**影响:** `test_full_pipeline.py` 全部失败  
**修复:** 下载数据文件或跳过相关测试

### 6. TFT 性能测试代码错误

**错误:**
```
TypeError: tuple indices must be integers or slices, not tuple
AttributeError: 'Output' object has no attribute 'shape'
```
**位置:** `tests/performance/test_tft_performance.py` (行 138, 186, 252)  
**影响:** 3 个 TFT 测试失败  
**修复:** 检查 `model()` 返回值类型，正确解包 output

---

## 🟡 P2 问题 (有空修复)

### 7. Performance 测试端口错误

**文件:** `tests/performance/test_baseline.py`, `tests/performance/test_perf_001.py`  
**问题:** 前端 URL 使用 `localhost:3000` 而非 `localhost:80`  
**影响:** 5 个性能测试失败  
**修复:** 同 E2E 测试，替换端口

---

## 📋 修复后验证步骤

```bash
# 1. 验证 Unit 测试
cd ~/qclaw
python3 -m pytest tests/unit/ -v

# 2. 验证 Integration 测试
python3 -m pytest tests/integration/ -v

# 3. 验证 E2E 测试
cd ~/qclaw/tests/e2e
npx playwright test --reporter=list

# 4. 验证 System 测试
cd ~/qclaw
python3 -m pytest tests/system/ -v

# 5. 验证 Performance 测试
python3 -m pytest tests/performance/ -v
```

---

## 📝 备注

- Functional 测试保持 100% 通过 ✅
- Docker 容器全部正常运行 (已运行 2 小时)
- 浏览器工具不可用，CODE-009 UI 验证暂时跳过

**状态:** 🔴 阻塞中 - 等待 Coder 修复 P0 问题  
**下次检查:** 2026-03-20 00:03 (Cron 自动)
